#include <stdio.h>
int main() {
    int array[7]={6,7,2,1,4,8,10};
     int i;
    for (i=0;i<7;i++)
    {
        printf("%d",array[i]);
    }
        printf("\n");
    for (i=6; i>=0; i--){
   
      printf("%d", array[i]);
   }
    return 0;
}
